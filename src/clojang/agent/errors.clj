(ns clojang.agent.errors)

(def nameserver-unresponsive
  "Nameserver not responding on spacemac when publishing clojang")

(def epmd-not-running?
  "EPMD doesn't seem to be running; has it been started?")
